This updates shared app code from the latest official LX Music mobile release while preserving the iOS project and build automation.

The upstream-sync workflow builds this branch explicitly. Review its build artifacts and test the music source, playback, synchronization and UI before merging. Successful compilation alone does not establish feature parity. Conflicts require manual porting; this PR is not configured to merge automatically.
